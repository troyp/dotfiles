#                                                                 -*- shell-script -*-
[[ ! -z $DEBUG  ]] && echo "sourcing .bash_profile"

# load .profile
[ -e ~/.profile ] && source ~/.profile

# -------------------------------------------------------------------------------
